function Palindrome(str) { 
  
  str = str.replace(/\s/g, '');
  
  for (var i =0; i < Math.floor(str.length/2); i++) {
    if ( str[i] != str[str.length - i -1] ){
        return false;
    }
  }

  // code goes here  
  return true; 
         
}
